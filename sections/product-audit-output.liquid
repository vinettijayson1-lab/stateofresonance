{% layout none %}Content-Type: application/json

[{% for product in collections.all.products %}{
"id":{{ product.id }},"title":{{ product.title | json }},"handle":{{ product.handle | json }},"has_image":{% if product.featured_image %}true{% else %}false{% endif %},"desc_len":{{ product.description | strip_html | strip | size }}
}{% unless forloop.last %},{% endunless %}{% endfor %}]
